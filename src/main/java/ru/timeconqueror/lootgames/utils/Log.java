package ru.timeconqueror.lootgames.utils;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class Log {
    public static final Logger DEBUGGER = LogManager.getLogger("LootGames Debugger");
    public static final Logger LOG_ROOM_HANDLER = LogManager.getLogger("LootGames Room Handler");
}
